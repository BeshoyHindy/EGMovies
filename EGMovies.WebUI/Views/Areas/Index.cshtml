@model IEnumerable<EGMovies.BOL.Cinema>

@{
    ViewBag.Title = "Search By Area Name";
}

<h2>Index</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@


    @using (Html.BeginForm("Index", "Areas", FormMethod.Get))
    {
        <div>
            <table>
                <tr>
                    <td><label> Search By Area Name : </label></td>
                    <td>@Html.DropDownList("id", "All")</td>
                    <td><input type="submit" value="Search" /></td>

                </tr>
            </table>
        </div>
    }
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CinemaGroupId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CinemaGroup.Name)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |*@
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @*@Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
        </td>
    </tr>
}

</table>
